<Page x:Class="Modrix.Views.Pages.WikiPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      Title="Wiki">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,12">
                <ui:SymbolIcon Symbol="BookInformation24" Width="32" Height="32" Margin="0,0,12,0" 
                              Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                <TextBlock Text="Knowledge Base" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Text="Find helpful information and explanations about Modrix features." 
                      FontSize="14" 
                      Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
        </StackPanel>
        
        <!-- Search Bar -->
        <Border Grid.Row="1" 
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                CornerRadius="8"
                Padding="16"
                Margin="0,0,0,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <ui:TextBox Grid.Column="0"
                           x:Name="SearchBox"
                           PlaceholderText="Search wiki entries..."
                           Icon="{ui:SymbolIcon Search24}"
                           TextChanged="SearchBox_TextChanged"/>
                
                <ui:Button Grid.Column="1"
                          Content="Clear"
                          Icon="{ui:SymbolIcon Delete24}"
                          Margin="8,0,0,0"
                          Click="ClearSearch_Click"/>
            </Grid>
        </Border>
        
        <!-- Content -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ContentPanel">
                <!-- Categories will be populated here -->
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>