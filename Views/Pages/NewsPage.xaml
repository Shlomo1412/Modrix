<Page
    x:Class="Modrix.Views.Pages.NewsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:helpers="clr-namespace:Modrix.Helpers"
    mc:Ignorable="d"
    Title="NewsPage"
    d:DesignHeight="600"
    d:DesignWidth="900"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}">

	<Page.Resources>
		<helpers:EmptyCollectionToVisibilityConverter x:Key="EmptyCollectionToVisibilityConverter"/>
	</Page.Resources>
	
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32,24,32,24">
            <!-- Latest Commits Section -->
            <Border Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="12" Padding="24" Margin="0,0,0,16">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,16">
                        <ui:Image Source="pack://application:,,,/Resources/Icons/ModrixGitHub.png" Width="32" Height="32"/>
                        <TextBlock Text="Latest Commits" FontSize="22" FontWeight="SemiBold" Margin="12,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <ItemsControl ItemsSource="{Binding LatestCommits}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource ControlFillColorSecondaryBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                                    <DockPanel>
                                        <StackPanel Orientation="Vertical" DockPanel.Dock="Left" Width="600">
                                            <TextBlock Text="{Binding Message}" FontWeight="Bold" FontSize="15" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding Author}" FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,4,0,0"/>
                                        </StackPanel>
                                        <ui:Button Icon="{ui:SymbolIcon Open24}" ToolTip="Open on GitHub" Width="40" Height="40" Command="{Binding DataContext.OpenCommitCommand, RelativeSource={RelativeSource AncestorType=Page}}" CommandParameter="{Binding Url}" Appearance="Secondary"/>
                                    </DockPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Text="No recent commits found." FontStyle="Italic" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Visibility="{Binding LatestCommits.Count, Converter={StaticResource EmptyCollectionToVisibilityConverter}}" HorizontalAlignment="Center" Margin="0,16,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
