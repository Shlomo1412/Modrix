<Page
    x:Class="Modrix.Views.Pages.ResourcePack.ModelsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Modrix.Views.Pages.ResourcePack"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:controls="clr-namespace:Modrix.Views.Controls"
    Title="Minecraft Models"
    d:DataContext="{d:DesignInstance Type=local:ModelsPage, IsDesignTimeCreatable=False}"
    d:DesignHeight="600"
    d:DesignWidth="900"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Grid Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource ControlFillColorDefaultBrush}" 
                CornerRadius="0,0,18,18" Padding="0,0,0,18" Margin="0,0,0,18">
            <Grid Margin="32,24,32,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontSize="22" FontWeight="Bold" Text="Minecraft Models" />
                        <controls:WikiTooltip 
                            WikiId="minecraft-models"
                            Title="Minecraft Models"
                            Category="Models"
                            Keywords="minecraft,models,json,blockbench,3d"
                            Description="Browse and edit Minecraft's model files to create custom item and block models. Model files define the 3D shape, textures, and display properties of blocks and items."
                            Margin="8,5,0,0"
                            VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Margin="0,2,0,0" FontSize="13" 
                               Foreground="{DynamicResource TextFillColorTertiaryBrush}" 
                               Text="Browse and create overrides from Minecraft model files" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="8,0,0,0" VerticalAlignment="Center">
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="32,0,32,32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Categories Sidebar -->
            <Border Grid.Column="0" Background="{DynamicResource ControlFillColorDefaultBrush}" 
                    CornerRadius="12" Padding="16" Margin="0,0,18,0"
                    Effect="{DynamicResource CardShadow}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Categories" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>

                    <ListBox x:Name="CategoriesList" Grid.Row="1" 
                             Background="Transparent" BorderThickness="0"
                             SelectionChanged="CategoriesList_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <ui:SymbolIcon Grid.Column="0" Symbol="{Binding Icon}" 
                                                  Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" 
                                              VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="{Binding Count}" 
                                              FontSize="10" 
                                              Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                              VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Main Content Area -->
            <Border Grid.Column="1" Background="{DynamicResource ControlFillColorDefaultBrush}" 
                    CornerRadius="12" Padding="24" 
                    Effect="{DynamicResource CardShadow}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Search and Controls -->
                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <ui:TextBox x:Name="SearchBox" 
                                   PlaceholderText="Search models..."
                                   Icon="{ui:SymbolIcon Search24}"
                                   Padding="36,8,12,8"
                                   MinHeight="36"
                                   TextChanged="SearchBox_TextChanged"/>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="12,0,0,0">
                            <ui:Button x:Name="ViewModeButton" 
                                      Icon="{ui:SymbolIcon GridDots24}"
                                      ToolTip="Toggle view mode"
                                      Click="ViewModeButton_Click"
                                      Margin="0,0,8,0"/>
                            <ui:Button Content="Refresh" 
                                      Icon="{ui:SymbolIcon ArrowClockwise24}"
                                      Click="RefreshModels_Click"/>
                        </StackPanel>
                    </Grid>

                    <!-- Models Display -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <Grid>
                            <!-- Loading State -->
                            <StackPanel x:Name="LoadingState" HorizontalAlignment="Center" 
                                       VerticalAlignment="Center" Visibility="Collapsed">
                                <ui:ProgressRing IsIndeterminate="True" Width="48" Height="48"/>
                                <TextBlock x:Name="LoadingText" Text="Loading models..." FontSize="16" 
                                          Margin="0,16,0,4" HorizontalAlignment="Center"/>
                                <TextBlock x:Name="LoadingSubtext" Text="Please wait while we load the model data..." 
                                          FontSize="14" Foreground="{DynamicResource TextFillColorTertiaryBrush}" 
                                          HorizontalAlignment="Center" TextAlignment="Center" MaxWidth="300" TextWrapping="Wrap"/>
                            </StackPanel>

                            <!-- Grid View (Default) -->
                            <Grid x:Name="GridView">
                                <ItemsControl x:Name="ModelsGrid">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{DynamicResource ControlFillColorSecondaryBrush}" 
                                                    CornerRadius="8" Margin="4" Padding="8" Width="120" Height="140"
                                                    ToolTip="{Binding Name}"
                                                    MouseLeftButtonDown="ModelItem_MouseLeftButtonDown">
                                                <Border.ContextMenu>
                                                    <ContextMenu>
                                                        <MenuItem Header="Create Override" Tag="{Binding}" Click="CreateOverride_Click"
                                                                  Icon="{ui:SymbolIcon Add24}"/>
                                                        <MenuItem Header="View JSON" Tag="{Binding}" Click="ViewJson_Click"
                                                                  Icon="{ui:SymbolIcon DocumentText24}"/>
                                                    </ContextMenu>
                                                </Border.ContextMenu>
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="80"/>
                                                        <RowDefinition Height="*"/>
                                                    </Grid.RowDefinitions>

                                                    <!-- Model Icon -->
                                                    <Border Grid.Row="0" Background="{DynamicResource AccentFillColorDefaultBrush}" 
                                                            CornerRadius="4" Width="64" Height="64" HorizontalAlignment="Center">
                                                        <ui:SymbolIcon Symbol="Cube24" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                                    </Border>

                                                    <!-- Model Name -->
                                                    <TextBlock Grid.Row="1" Text="{Binding Name}" 
                                                              FontSize="10" TextWrapping="Wrap"
                                                              HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"
                                                              TextAlignment="Center"
                                                              Margin="0,4,0,0"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>

                            <!-- List View -->
                            <Grid x:Name="ListView" Visibility="Collapsed">
                                <ui:DataGrid x:Name="ModelsList" 
                                            Background="Transparent" 
                                            BorderThickness="0"
                                            MouseLeftButtonDown="ModelItem_MouseLeftButtonDown"
                                            AutoGenerateColumns="False"
                                            CanUserAddRows="False"
                                            CanUserDeleteRows="False"
                                            CanUserResizeRows="False"
                                            GridLinesVisibility="None"
                                            HeadersVisibility="Column">
                                    <ui:DataGrid.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Create Override" Tag="{Binding}" Click="CreateOverride_Click"
                                                      Icon="{ui:SymbolIcon Add24}"/>
                                            <MenuItem Header="View JSON" Tag="{Binding}" Click="ViewJson_Click"
                                                      Icon="{ui:SymbolIcon DocumentText24}"/>
                                        </ContextMenu>
                                    </ui:DataGrid.ContextMenu>
                                    <ui:DataGrid.Columns>
                                        <DataGridTextColumn Header="Name" Width="200" Binding="{Binding Name}"/>
                                        <DataGridTextColumn Header="Category" Width="100" Binding="{Binding Category}"/>
                                        <DataGridTextColumn Header="Size" Width="80" Binding="{Binding Size}"/>
                                        <DataGridTemplateColumn Header="Actions" Width="120">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <ui:Button Content="Override" Tag="{Binding}" 
                                                                  Click="CreateOverride_Click" 
                                                                  Padding="8,4" Height="28" Margin="0,0,4,0"/>
                                                        <ui:Button Content="View" Tag="{Binding}" 
                                                                  Click="ViewJson_Click" 
                                                                  Padding="8,4" Height="28"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </ui:DataGrid.Columns>
                                </ui:DataGrid>
                            </Grid>

                            <!-- Empty State -->
                            <StackPanel x:Name="EmptyState" HorizontalAlignment="Center" 
                                       VerticalAlignment="Center" Visibility="Collapsed">
                                <ui:SymbolIcon Symbol="Cube48" FontSize="48" 
                                              Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                                <TextBlock Text="No models found" FontSize="16" FontWeight="SemiBold" 
                                          Margin="0,8,0,4" HorizontalAlignment="Center"/>
                                <TextBlock Text="Extract Minecraft assets or check your installation" 
                                          FontSize="14" Foreground="{DynamicResource TextFillColorTertiaryBrush}" 
                                          HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>