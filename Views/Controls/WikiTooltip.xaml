<UserControl x:Class="Modrix.Views.Controls.WikiTooltip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             Width="Auto"
             Height="Auto"
             x:Name="WikiTooltipControl">

    <UserControl.Resources>
        <!-- Tooltip styled to match ProjectCard flyout with hardcoded colors -->
        <Style x:Key="WikiTooltipStyle" TargetType="ToolTip">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background" Value="#2C2D35"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="MaxWidth" Value="460"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="HasDropShadow" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border CornerRadius="10"
                                Background="{TemplateBinding Background}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.35"/>
                            </Border.Effect>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Header -->
                                <Border Grid.Row="0"
                                        CornerRadius="10,10,0,0" 
                                        Background="#3C3D45" 
                                        Padding="10,8,10,8">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Rectangle Width="4" Height="28" RadiusX="2" RadiusY="2" 
                                                   Fill="#4C7DFF" 
                                                   Margin="0,0,10,0"/>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}" 
                                                       FontWeight="SemiBold" 
                                                       FontSize="14"
                                                       Foreground="#FFFFFF" />
                                            <TextBlock Margin="0,-2,0,0" 
                                                       FontSize="11" 
                                                       Foreground="#B0B6C3"
                                                       Text="{Binding Category}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>

                                <Separator Grid.Row="1" Margin="0" Background="#4C4D55" />

                                <!-- Content -->
                                <StackPanel Grid.Row="2" Margin="8" MinWidth="210">
                                    <TextBlock Text="{Binding Description}"
                                               TextWrapping="Wrap"
                                               LineHeight="20"
                                               Margin="10,6"
                                               Foreground="#FFFFFF"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ui:Button Width="20"
               Height="20"
               Padding="0"
               Appearance="Transparent"
               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
               ToolTipService.InitialShowDelay="300"
               ToolTipService.ShowDuration="10000"
               ToolTipService.BetweenShowDelay="100"
               Tag="{Binding RelativeSource={RelativeSource AncestorType=UserControl}}">
        <ui:Button.Icon>
            <ui:SymbolIcon Symbol="QuestionCircle24" />
        </ui:Button.Icon>
        <ui:Button.ToolTip>
            <ToolTip Style="{StaticResource WikiTooltipStyle}">
                <ToolTip.DataContext>
                    <Binding RelativeSource="{RelativeSource Self}" Path="PlacementTarget.Tag" />
                </ToolTip.DataContext>
            </ToolTip>
        </ui:Button.ToolTip>
    </ui:Button>
</UserControl>
