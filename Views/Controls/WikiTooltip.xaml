<UserControl x:Class="Modrix.Views.Controls.WikiTooltip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="24" d:DesignWidth="24">
    <Grid>
        <Border Background="Transparent">
            <wpfui:Button x:Name="HelpButton"
                          Appearance="Secondary"
                          Width="22" Height="22" Padding="0"
                          ToolTipService.ShowDuration="2147483647"
                          Tag="{Binding RelativeSource={RelativeSource AncestorType=UserControl}}">
                <wpfui:Button.Icon>
                    <wpfui:SymbolIcon Symbol="Info24" />
                </wpfui:Button.Icon>
                <wpfui:Button.ToolTip>
                    <ToolTip Placement="Mouse"
                             MaxWidth="420"
                             Background="{DynamicResource ApplicationBackgroundBrush}"
                             Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                             HasDropShadow="True"
                             DataContext="{Binding PlacementTarget, RelativeSource={RelativeSource Self}}">
                        <Border Padding="12" CornerRadius="8" Background="{DynamicResource ControlFillColorDefaultBrush}" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="{Binding Tag.Title}" FontWeight="Bold" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding Tag.Category}" Foreground="Gray" FontSize="11"/>
                                <Separator Margin="0,6,0,6"/>
                                <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                                    <TextBlock TextWrapping="Wrap" Text="{Binding Tag.Summary}"/>
                                </ScrollViewer>
                                <ItemsControl ItemsSource="{Binding Tag.Keywords}" Margin="0,6,0,0">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#1F6AD1" CornerRadius="4" Padding="4,2" Margin="0,4,4,0">
                                                <TextBlock Text="{Binding}" Foreground="White" FontSize="11"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </ToolTip>
                </wpfui:Button.ToolTip>
            </wpfui:Button>
        </Border>
    </Grid>
</UserControl>